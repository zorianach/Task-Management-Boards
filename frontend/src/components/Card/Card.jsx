import React from 'react'
// import data from '../../issues.json'

const Card = () => {
    return (
        <>Card</>
    )
    // data.map((el, ind) => (
	// 	<div className='container' key={el.id}>
	// 		<h1>{el.title}</h1>
	// 		<h5>{el.body}</h5>
	// 	</div>
	// ))
}

export default Card